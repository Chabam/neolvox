name: Standalone build and test
on:
   workflow_dispatch:

jobs:
  call-build:
    uses: ./.github/workflows/ci-standalone.yml
    with:
      do_release: false
      do_testing: true
    permissions:
      contents: read
      checks: write
      pull-requests: write

